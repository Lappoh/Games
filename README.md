# Restaurant Order & Analytics System  🍽️

## Описание проекта
Данный проект представляет собой интегрированную систему для автоматизации работы ресторана с доставкой. 
Система включает Telegram-бота для приема заказов и веб-дашборд для аналитики в реальном времени.

**Основные возможности:**
*   🤖 **Telegram бот:** Полный цикл оформления заказа (выбор блюд, ввод данных, подтверждение)
*   📊 **Интерактивный дашборд:** Аналитика продаж в реальном времени
*   🗄️ **База данных:** Хранение заказов и меню
*   📈 **Автообновление:** Данные обновляются автоматически каждые 30 секунд

## Установка и запуск

### Предварительные требования
*   Python 3.9+
*   Git
*   Аккаунт Telegram и токен бота от @BotFather

### Инструкция
1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/your-username/restaurant-order-system.git
    cd restaurant-order-system
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    python -m venv venv
    # Windows
    venv\Scripts\activate
    # Mac/Linux
    source venv/bin/activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Настройка переменных окружения:**
    Создайте файл `.env` и добавьте ваш токен:
    ```
    TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
    ```

5. Инициализация базы данных:
    ```bash
    python database.py
    ```
    Эта команда создаст базу данных restaurant.db и заполнит ее тестовым меню.

6.  Запуск Telegram бота:
    ```bash
    python telegram_bot_simple.py
    ```
    Бот запустится и будет готов принимать заказы. Найдите его в Telegram по имени и начните диалог.

7.  Запуск аналитического дашборда:
    ```bash
    python dashboard.py
    ```
    Дашборд будет доступен по адресу: http://localhost:8050
    
5.  **Запуск бота:**
    ```bash
    python main.py
    ```
6. **Запуск Дашборда:**
    ```bash
    python -m streamlit run dashboard.py
    ```

## Использование системы
1.  Найдите бота в Telegram по имени `@FoodZakazi_bot`.
2.  Нажмите `/start`.
3.  Используйте команды:
*   `/menu` - просмотр меню ресторана
*   `/order` - начать оформление заказа
*   `/help` - справка по командам
